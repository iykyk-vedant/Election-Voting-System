services:
  - type: web
    name: election-voting-system
    env: node
    buildCommand: npm install
    startCommand: PORT=10000 node server.js
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: PORT
        value: 10000